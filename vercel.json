{
  "version": 2,
  "functions": {
    "api/webhook.js": {
      "runtime": "nodejs20.x"
    }
  },
  "builds": [
    { "src": "api/webhook.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" }
  ]
}
